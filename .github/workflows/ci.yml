# Ember CI
#
# Design:
#   - `ci-ok` is the single required status check. It ALWAYS runs (even when
#     upstream jobs are skipped) and fails only if a job it depends on failed
#     or was cancelled. Mark `ci-ok` as required in branch protection; never
#     mark the individual jobs.
#   - `changes` is a stub for per-area path filtering. Today it reports every
#     area as changed. When real build/test jobs land, replace the stub with a
#     path filter and gate each job on the matching output so unrelated areas
#     are skipped (skipped jobs still let `ci-ok` pass).

name: ci

on:
  pull_request:
    branches: [main, develop]
  push:
    branches: [main, develop]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  changes:
    name: detect changes
    runs-on: ubuntu-latest
    outputs:
      web: ${{ steps.filter.outputs.web }}
      cli: ${{ steps.filter.outputs.cli }}
      mcp: ${{ steps.filter.outputs.mcp }}
      pipeline: ${{ steps.filter.outputs.pipeline }}
      packages: ${{ steps.filter.outputs.packages }}
      infra: ${{ steps.filter.outputs.infra }}
    steps:
      # TODO(EMBER-28 follow-up): replace with a real path filter, e.g.
      #   uses: dorny/paths-filter@<pinned-sha>
      #   with:
      #     filters: |
      #       web:      ['apps/web/**', 'packages/**']
      #       cli:      ['apps/cli/**', 'packages/**']
      #       mcp:      ['apps/mcp/**', 'packages/**']
      #       pipeline: ['services/pipeline/**', 'packages/**']
      #       packages: ['packages/**']
      #       infra:    ['infra/**', '.github/**']
      - name: Stub path filter (everything marked changed)
        id: filter
        run: |
          for area in web cli mcp pipeline packages infra; do
            echo "$area=true" >> "$GITHUB_OUTPUT"
          done

  sanity:
    name: sanity
    runs-on: ubuntu-latest
    needs: changes
    steps:
      - uses: actions/checkout@v4
      - name: Repo layout present
        run: |
          set -euo pipefail
          for p in apps/web apps/cli apps/mcp services/pipeline services/github-ingestion packages infra docs/adr .github/PULL_REQUEST_TEMPLATE.md .github/CODEOWNERS; do
            test -e "$p" || { echo "missing: $p"; exit 1; }
          done
          echo "layout ok"
      - name: docker-compose is valid YAML
        run: docker compose -f infra/docker-compose.yml config --quiet
      - name: Ingestion envelope contract
        run: python3 packages/ingestion-envelope/test_contract.py
      - name: GitHub ingestion webhook contract
        run: python3 services/github-ingestion/test_webhook.py

  # Placeholder for future per-area jobs. Gate each on the matching `changes`
  # output, e.g.:
  #
  #   web:
  #     needs: changes
  #     if: needs.changes.outputs.web == 'true'
  #     ...
  #
  # and add the job name to `ci-ok.needs`.

  ci-ok:
    name: ci-ok
    runs-on: ubuntu-latest
    if: always()
    needs:
      - changes
      - sanity
    steps:
      - name: Evaluate upstream job results
        env:
          RESULTS: ${{ toJSON(needs) }}
        run: |
          set -euo pipefail
          echo "$RESULTS"
          if echo "$RESULTS" | grep -Eq '"result": *"(failure|cancelled)"'; then
            echo "::error::one or more upstream jobs failed or were cancelled"
            exit 1
          fi
          echo "ci-ok"
