{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "envelope.v1.schema.json",
  "title": "Ember ingestion intake v1",
  "description": "Shared intake for raw connector data. Workers emit {type, body}. A later processing pipeline cleans and sorts. Embeddings are not part of this contract (EMBER-3).",
  "type": "object",
  "additionalProperties": false,
  "required": ["type", "body"],
  "properties": {
    "type": {
      "type": "string",
      "enum": ["github", "jira", "slack", "teams", "gitlab"],
      "description": "Ingestion source that produced body."
    },
    "body": {
      "type": "object",
      "description": "Opaque raw connector JSON. Any object. Not a typed per-source payload."
    }
  }
}
